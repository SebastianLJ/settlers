package model.board;

public class Hex {
    private Terrain terrain;
    private int numberToken;
    private Intersection[] intersections = new Intersection[5];
    private Path[] paths = new Path[6];

    public Hex(Terrain terrain, int numberToken) {
        
    }
}

